import React from 'react';
import { View } from 'react-native';

// Placeholder screen so the Tabs.Screen name="new" is registered.
// We don't navigate here because the custom tab button opens an ActionSheet.
export default function NewTabPlaceholder() {
  return <View />;
}
